import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CountryprofileaccordionPage } from './countryprofileaccordion.page';

describe('CountryprofileaccordionPage', () => {
  let component: CountryprofileaccordionPage;
  let fixture: ComponentFixture<CountryprofileaccordionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CountryprofileaccordionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
